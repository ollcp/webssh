on:
  push:
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: BUILD
        run: localbuild.sh
      - name: RPM Build
        uses: naveenrajm7/rpmbuild@v1.0.0
        with:
          spec_file: webssh.spec
